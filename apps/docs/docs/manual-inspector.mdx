---
title: Manual Inspector
description: Explore the device UI, build shell scripts, and save actions without writing locator code.
---

The Manual Inspector is a desktop surface for **exploring** a connected device the way a QA engineer would, then turning what you did into a reusable script.

## Flow

<Steps>
  <Step title="Connect a device">
    Select an iOS or Android device/simulator so an Appium session is active.
  </Step>

  <Step title="Inspect">
    View the live screen. Select elements to target actions (tap, swipe, input, lifecycle).
  </Step>

  <Step title="Build a shell script">
    Actions append to a script (CLI-shaped commands) instead of requiring you to invent locators by hand.
  </Step>

  <Step title="Run or save">
    Execute the script against the session, export a report, or keep it for later regression.
  </Step>
</Steps>

## Live mirror and control

Inspector shows the device screen as a live MJPEG feed on iOS Simulator, real iOS, and Android. A badge on the mirror tells you which transport is active:

- **Stream** — MJPEG feed from the Appium driver. This is the default when the stream is reachable.
- **Poll** — fallback that refreshes a screenshot every ~250 ms. Inspector shows this if the MJPEG probe fails after connect.

Toggle **Live control** above the mirror to drag directly on the device:

- **On** — pointer drags on the mirror map to touch events on the device (tap, drag, swipe). Element selection and the action menu are paused while you drag.
- **Off** — clicking an element opens the action menu so you can insert or run a scripted step.

Live control is exclusive with running scripts. Inspector blocks pointer input while a script step is in flight so gestures do not interleave with the running script.

The live mirror never writes to disk. Explicit screenshots and script reports still persist to `~/.yoqa/runs/screenshots/`.

### Recovering a dropped session

Real iOS sessions can occasionally drop under WDA load (for example, during long Inspector sessions with the live mirror running). When that happens, Inspector clears the feed and prompts you to reconnect manually — it does not attempt to reconnect on its own.

- **Restart session** — the toolbar button above the mirror disconnects, reconnects, and remounts the live stream. Use it when Inspector shows the Restart prompt, or any time the mirror looks stuck.
- **Connect** — pick the device again in the device selector to start a fresh session.

## When to use it

- Learn screen structure before writing natural-language cases.
- Reproduce a bug with an exact sequence of gestures.
- Produce a **saved script** after you understand the happy path (complementary to AI agent runs).

For autonomous NL cases, use [Test Cases](/docs/test-cases) + Runs. For agent-driven debugging from the terminal, use [CLI for agents](/guide/cli-for-agents).

<CardGroup cols={2}>
  <Card title="Desktop app" icon="desktop" href="/docs/desktop-app">
    Where to open Inspector
  </Card>

  <Card title="CLI" icon="terminal" href="/docs/cli">
    Equivalent screen / action commands
  </Card>
</CardGroup>
